# Production / VPS. Vite loads this for `npm run build`.
# Copy to .env.production, then fill in the API URL:
#   cp .env.production.example .env.production
#
# Vercel: paste the same key/value in Project Settings instead of this file.

VITE_API_BASE_URL=https://api.your-domain.com
