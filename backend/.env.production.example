# Production / VPS. Copy this file onto the server as .env.production, then replace the placeholders:
#   cp .env.production.example .env.production
#
# Render: paste the same keys in the dashboard instead of this file.

NODE_ENV=production
PORT=4062
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/website-builder-demo
FRONTEND_ORIGIN=https://webloom.abhiiiijain.com,https://www.webloom.abhiiiijain.com
